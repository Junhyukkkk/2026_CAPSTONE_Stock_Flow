# Kafka
confluent-kafka==2.15.1

# WebSocket
# 주의: Dockerfile이 python:3.9-slim을 사용 중이므로, Python 3.9를 지원하는
# 마지막 major 버전(15.x)에 고정한다. websockets>=16.0은 Python 3.10+를
# 요구하므로 Dockerfile의 베이스 이미지를 올리지 않는 한 업그레이드하지 말 것.
# 15.x부터 InvalidStatusCode 대신 InvalidStatus를 사용한다(각 producer 참고).
websockets==15.0.1

# HTTP 요청
# 주의: requests>=2.33.0은 Python 3.10+를 요구한다. Python 3.9 호환 마지막 버전에 고정.
requests==2.32.5

# 환경 변수 관리
# 주의: python-dotenv>=1.2.2는 Python 3.10+를 요구한다. Python 3.9 호환 마지막 버전에 고정.
python-dotenv==1.2.1
