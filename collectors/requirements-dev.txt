# 테스트 전용 의존성 (프로덕션 이미지에는 포함하지 않음)
# 사용법: pip install -r requirements.txt -r requirements-dev.txt && pytest tests/
-r requirements.txt

# 주의: pytest>=9.0.0은 Python 3.10+를 요구한다. Dockerfile의 python:3.9-slim과
# 호환되는 마지막 버전에 고정.
pytest==8.4.2
