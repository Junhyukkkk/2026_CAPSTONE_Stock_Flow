global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: 'stockflow-realtime'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['stockflow-realtime:8081']
    scrape_interval: 5s
