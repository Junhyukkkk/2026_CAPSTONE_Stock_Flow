# 로컬(호스트)에서 직접 실행할 때 — 호스트에 매핑된 TimescaleDB 포트(5433)
DB_HOST=localhost
DB_PORT=5433
DB_NAME=stockflow
DB_USERNAME=postgres
DB_PASSWORD=postgres

# 모델 저장 경로
MODEL_DIR=models

# 기본 예측 데이터 단위: 1m(분봉) | 1d(일봉)
DEFAULT_INTERVAL=1m
# DB에서 가져올 최근 캔들 수
DEFAULT_LIMIT=2000
